__________________________________

Application platforms for Kotlin: 

JVM
Android
JavaScript (Browser)
Native
__________________________________

nextLine() читает всю строку до символа новой строки (\n) и возвращает её 
(без самого символа перевода строки). 

Она не пропускает пустые строки в отличие от next().
__________________________________